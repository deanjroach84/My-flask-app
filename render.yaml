services:
  - type: web
    name: Scanner_Tool
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 10000
